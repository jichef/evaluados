<%- include('partials/header') %>

<div class="d-flex flex-column justify-content-center align-items-center" style="min-height: 80vh">
  <div class="text-center">
    <h1 class="display-1 fw-bold text-muted"><%= code || 500 %></h1>
    <h2 class="h4 mb-3"><%= title || 'Error' %></h2>
    <p class="text-muted mb-4"><%= message || 'Ha ocurrido un error inesperado.' %></p>
    <a href="/" class="btn btn-primary">Volver al inicio</a>
  </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body></html>
